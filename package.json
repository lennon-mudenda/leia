{
  "name": "leia",
  "version": "1.0.0",
  "description": "A simple library to allow using CSS in React-Native Projects",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["React", "StyleSheets", "CSS", "CSS to React Style Objects"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "css": "^3.0.0"
  },
  "devDependencies": {
    "jest": "^26.6.3"
  }
}
